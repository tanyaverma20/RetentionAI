# Models

Store only approved, versioned model artifacts and metadata according to the model-governance process. Large model binaries are ignored by Git. The active model reference must identify its version, training data provenance, and evaluation metrics.
